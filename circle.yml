machine:
  java:
    version:
      oraclejdk8
  pre:
	- curl -s https://get.sdkman.io | bash
	- sdk install gradle 2.14
deployment:
   staging:
     branch: master
     heroku:
       appname: spottr-server
