machine:
  java:
    version:
      openjdk8
dependencies:
  pre:
    - chmod +x setGradle.sh
    - chmod +x gradle.sh
    - ./setGradle.sh
deployment:
   staging:
     branch: master
     heroku:
       appname: spottr-server
